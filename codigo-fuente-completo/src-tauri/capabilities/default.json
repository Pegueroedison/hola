{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos por defecto para ERP POS desktop",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "sql:default"
  ]
}
